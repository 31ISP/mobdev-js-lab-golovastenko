# Лабораторная работа: Основы JavaScript

> **Цель:** освоить работу с числами, строками, массивами и объектами в JavaScript

## Запуск JS через Node.js

Node.js позволяет запускать JavaScript прямо в терминале, без браузера.

**Запуск файла:**
```bash
node имя_файла.js
```

Создайте файл `index.js`, напишите туда код и запустите через терминал. Вывод в консоль делается через `console.log()` — это аналог `print()` из других языков.

```javascript
console.log("Привет, мир!")      // Привет, мир!
console.log(2 + 2)               // 4
console.log(typeof "hello")      // string
```

---

## Раздел 1. Числа

### Теория

В JavaScript все числа — один тип `number`, неважно целое это или дробное.

**Базовая арифметика** работает стандартно: `+`, `-`, `*`, `/`. Оператор `%` возвращает остаток от деления, `**` — возведение в степень.

```javascript
console.log(10 % 3)   // 1 — остаток от деления
console.log(2 ** 8)   // 256 — два в восьмой степени
```

**`NaN` (Not a Number)** — специальное значение, которое возникает при некорректных математических операциях. Важная особенность: `NaN !== NaN`, поэтому для проверки используют `isNaN()`.

```javascript
console.log("текст" * 2)       // NaN
console.log(isNaN("текст" * 2)) // true
```

**`Math`** — встроенный объект с математическими функциями:

| Метод | Что делает |
|---|---|
| `Math.round(x)` | Округляет до ближайшего целого |
| `Math.floor(x)` | Округляет вниз |
| `Math.ceil(x)` | Округляет вверх |
| `Math.abs(x)` | Модуль числа |
| `Math.max(a, b, ...)` | Максимум из чисел |
| `Math.min(a, b, ...)` | Минимум из чисел |
| `Math.random()` | Случайное число от 0 до 1 |
| `Math.sqrt(x)` | Квадратный корень |

`toFixed(n)` — метод числа, возвращает строку с `n` знаками после запятой:

```javascript
const price = 19.9876
console.log(price.toFixed(2))  // "19.99"
```

**Преобразование строки в число:**

```javascript
Number("42")      // 42
parseInt("42px")  // 42 — берёт целую часть до нецифрового символа
parseFloat("3.14abc") // 3.14
```

---

### Задание 1

Создайте файл `numbers.js` и выполните следующее:

1. Объявите переменную `temperature` со значением `36.6789`. Выведите её, округлённую до 1 знака после запятой.
2. Объявите две переменные `a = 17` и `b = 5`. Выведите результат целочисленного деления (подсказка: `Math.floor`) и остаток от деления.
3. Напишите выражение, которое генерирует случайное целое число от 1 до 10 включительно. Выведите результат. *(Подсказка: `Math.random()` даёт число от 0 до 1, `Math.floor` и умножение помогут получить целое)*
4. Проверьте с помощью `isNaN`, является ли результатом числом: `"100" / 5`, `"сто" / 5`. Выведите результаты проверки.
5. Используя `Math.max` и `Math.min`, найдите максимальное и минимальное из чисел: `14, -3, 77, 0, -100, 42`.

---

## Раздел 2. Строки

### Теория

Строки в JS можно объявлять через `'одинарные'`, `"двойные"` или `` `обратные кавычки` ``. Обратные кавычки — это **шаблонные литералы**: в них можно вставлять выражения через `${}`.

```javascript
const name = "Мария"
const age = 21
console.log(`Студентка ${name}, возраст: ${age} лет`)
// Студентка Мария, возраст: 21 лет
```

Внутри `${}` можно писать любое JS-выражение, не только переменную:

```javascript
console.log(`Сумма: ${10 + 20}`)   // Сумма: 30
console.log(`Это ${age >= 18 ? "совершеннолетний" : "несовершеннолетний"}`)
```

**Полезные методы строк:**

| Метод | Что делает |
|---|---|
| `.length` | Длина строки (свойство, не метод) |
| `.toUpperCase()` | В верхний регистр |
| `.toLowerCase()` | В нижний регистр |
| `.trim()` | Убирает пробелы по краям |
| `.includes(str)` | Содержит ли подстроку → `true/false` |
| `.startsWith(str)` | Начинается ли с подстроки |
| `.endsWith(str)` | Заканчивается ли подстрокой |
| `.indexOf(str)` | Индекс первого вхождения, или `-1` |
| `.slice(start, end)` | Вырезает подстроку по индексам |
| `.replace(old, new)` | Заменяет первое вхождение |
| `.replaceAll(old, new)` | Заменяет все вхождения |
| `.split(sep)` | Разбивает строку в массив |
| `.repeat(n)` | Повторяет строку n раз |
| `.padStart(n, char)` | Дополняет строку слева до длины n |

```javascript
const str = "  Hello, World!  "
console.log(str.trim())                   // "Hello, World!"
console.log(str.trim().toLowerCase())     // "hello, world!"
console.log("ha".repeat(3))              // "hahaha"
console.log("7".padStart(3, "0"))        // "007"
```

Строки индексируются с нуля. Можно обращаться к символу через `str[i]` или `str.at(i)` — метод `at` поддерживает отрицательные индексы (`str.at(-1)` — последний символ).

---

### Задание 2

Создайте файл `strings.js` и выполните следующее:

1. Объявите переменную `sentence = "  javascript — это просто!  "`. Выведите строку без лишних пробелов и с первой заглавной буквой. *(Подсказка: `trim()`, `toUpperCase()` на первом символе и `slice()`)*
2. Напишите шаблонный литерал, который формирует строку вида: `"Студент Иванов Иван, группа ИСР-21, средний балл: 4.75"` — используя отдельные переменные для каждого значения.
3. Дана строка `"2025-03-15"` (дата в формате ГГГГ-ММ-ДД). Разбейте её через `split` и соберите в формат `"15.03.2025"` через шаблонный литерал.
4. Проверьте, начинается ли строка `"Node.js разработчик"` со слова `"Node"` и заканчивается ли на `"разработчик"`. Выведите результат.
5. Есть номер счёта — число `42`. Выведите его в формате с ведущими нулями до 8 символов: `"00000042"`.

---

## Раздел 3. Массивы

### Теория

Массив — упорядоченный список значений. Индексация с нуля.

```javascript
const fruits = ["яблоко", "банан", "вишня"]
console.log(fruits[0])      // "яблоко"
console.log(fruits.length)  // 3
```

**Методы изменения массива:**

| Метод | Что делает |
|---|---|
| `.push(el)` | Добавляет в конец |
| `.pop()` | Удаляет и возвращает последний |
| `.unshift(el)` | Добавляет в начало |
| `.shift()` | Удаляет и возвращает первый |
| `.splice(i, n)` | Удаляет `n` элементов начиная с `i` |

**Методы перебора — возвращают новый массив:**

`.map(fn)` — преобразует каждый элемент. Возвращает новый массив той же длины.

```javascript
// Каждое число возводим в квадрат
const nums = [1, 2, 3, 4]
const squared = nums.map(n => n ** 2)
console.log(squared)  // [1, 4, 9, 16]
```

`.filter(fn)` — оставляет только те элементы, для которых функция вернула `true`.

```javascript
// Только чётные числа
const evens = nums.filter(n => n % 2 === 0)
console.log(evens)  // [2, 4]
```

`.reduce(fn, initial)` — сворачивает массив в одно значение. Функция принимает аккумулятор и текущий элемент.

```javascript
// Сумма всех элементов
const sum = nums.reduce((acc, n) => acc + n, 0)
console.log(sum)  // 10
```

**Другие полезные методы:**

| Метод | Что делает |
|---|---|
| `.find(fn)` | Возвращает первый подходящий элемент |
| `.findIndex(fn)` | Индекс первого подходящего |
| `.some(fn)` | Хоть один подходит? → `true/false` |
| `.every(fn)` | Все подходят? → `true/false` |
| `.includes(el)` | Содержит элемент? → `true/false` |
| `.sort(fn)` | Сортирует массив (изменяет исходный!) |
| `.reverse()` | Разворачивает массив |
| `.join(sep)` | Объединяет в строку |
| `.flat()` | Разворачивает вложенные массивы |
| `.slice(start, end)` | Копирует часть массива |

> ⚠️ **Важно:** `sort()` без аргументов сортирует как строки. Для чисел передавайте функцию:
> ```javascript
> [10, 2, 1, 20].sort((a, b) => a - b)  // [1, 2, 10, 20]
> ```

---

### Задание 3

Создайте файл `arrays.js` и выполните следующее:

1. Дан массив оценок студента: `[4, 5, 3, 5, 2, 4, 5, 3]`. Вычислите средний балл с помощью `reduce`. Выведите результат, округлённый до 2 знаков.
2. Используя `filter`, получите из того же массива только оценки выше 3. Затем с помощью `map` замените каждую оценку строкой вида `"Оценка: 5"`.
3. Дан массив строк: `["банан", "яблоко", "вишня", "абрикос", "груша"]`. Отсортируйте его в алфавитном порядке и выведите результат через запятую (`.join`).
4. Дан массив товаров:
    ```javascript
    const products = [
      { name: "Ноутбук", price: 75000 },
      { name: "Мышь", price: 1500 },
      { name: "Монитор", price: 32000 },
      { name: "Клавиатура", price: 3500 },
      { name: "Наушники", price: 8000 }
    ]
    ```
    - С помощью `filter` получите товары дешевле 10 000 ₽.
    - С помощью `map` из исходного массива получите только названия товаров.
    - С помощью `reduce` посчитайте суммарную стоимость всех товаров.

5. Проверьте с помощью `some` и `every`: есть ли в массиве `[12, 45, 7, 33, 18]` хоть одно число больше 40? Все ли числа больше 5?

---

## Раздел 4. Объекты

### Теория

Объект — набор пар «ключ: значение». Значением может быть что угодно, включая функции (тогда это называется **методом** объекта).

```javascript
const student = {
  name: "Алексей",
  age: 20,
  grade: 4.5,
  greet() {
    return `Привет, я ${this.name}`  // this — это сам объект
  }
}

console.log(student.name)      // "Алексей" — через точку
console.log(student["age"])    // 20 — через скобки (полезно для динамических ключей)
console.log(student.greet())   // "Привет, я Алексей"
```

**Работа с ключами и значениями:**

```javascript
Object.keys(student)    // ["name", "age", "grade", "greet"]
Object.values(student)  // ["Алексей", 20, 4.5, ƒ]
Object.entries(student) // [["name", "Алексей"], ["age", 20], ...]
```

**Деструктуризация** — удобный способ извлечь значения из объекта в переменные:

```javascript
const { name, age } = student
console.log(name)  // "Алексей"
console.log(age)   // 20

// Можно сразу переименовать:
const { name: studentName } = student
console.log(studentName)  // "Алексей"

// Можно задать значение по умолчанию:
const { city = "Москва" } = student
console.log(city)  // "Москва" — ключа нет, используется дефолт
```

**Spread-оператор `...`** — копирует свойства одного объекта в другой:

```javascript
const base = { role: "user", active: true }
const admin = { ...base, role: "admin", name: "Петр" }
// { role: "admin", active: true, name: "Петр" }
```

**Добавление и удаление свойств:**

```javascript
student.email = "alex@mail.ru"  // добавить
delete student.grade            // удалить
```

**Проверка наличия ключа:**

```javascript
console.log("name" in student)              // true
console.log(student.hasOwnProperty("age"))  // true
```

---

### Задание 4

Создайте файл `objects.js` и выполните следующее:

1. Создайте объект `book` с полями: `title` (название), `author`, `year`, `pages`, `available` (булевое). Добавьте метод `info()`, который возвращает строку вида: `"«Мастер и Маргарита», Булгаков М.А., 1967 г., 480 стр."`. Вызовите метод и выведите результат.

2. С помощью деструктуризации извлеките из объекта `book` поля `title` и `author`. Для поля `rating` задайте значение по умолчанию `0`. Выведите все три переменные.

3. Дан массив объектов-студентов:
    ```javascript
    const students = [
      { name: "Анна", grade: 4.8, passed: true },
      { name: "Борис", grade: 2.9, passed: false },
      { name: "Вера", grade: 3.5, passed: true },
      { name: "Григорий", grade: 4.1, passed: true },
      { name: "Дарья", grade: 2.5, passed: false },
    ]
    ```
    - Выведите имена всех сдавших (`passed: true`) через `filter` и `map`.
    - Найдите студента с помощью `find` у которого оценка выше 4.5.
    - С помощью `reduce` посчитайте средний балл по всей группе.

4. Создайте функцию `mergeUsers(user1, user2)`, которая принимает два объекта и возвращает новый объект, объединяющий их свойства через spread-оператор. Если ключи совпадают — приоритет у `user2`. Проверьте на примере:
    ```javascript
    const u1 = { name: "Иван", age: 20, city: "Казань" }
    const u2 = { name: "Иван Петров", email: "ivan@mail.ru", city: "Москва" }
    ```

5. Напишите функцию `countByField(arr, field)`, которая принимает массив объектов и имя поля, и возвращает объект-счётчик: сколько раз встречается каждое значение этого поля. Проверьте на массиве `students` из задания 3 по полю `passed`.
    ```javascript
    // Ожидаемый результат:
    // { true: 3, false: 2 }
    ```

# Как сдавать

- Создайте форк репозитория в вашей организации с названием-этого-репозитория-вашафамилия
- Используя ветку wip сделайте задание
- Зафиксируйте изменения в вашем репозитории
- Когда документ будет готов - создайте пул реквест из ветки wip (вашей) на ветку main (тоже вашу) и укажите меня (ktkv419) как reviewer

Не мержите сами коммит, это сделаю я после проверки задания
